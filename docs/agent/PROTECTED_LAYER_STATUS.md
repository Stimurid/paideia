# Protected Layer Status

## Current Status

- installed: yes.
- protected: partial / strong-context.
- behaves: no.
- evolves: no.

## Protected By Context Now

- root `AGENTS.md`;
- root `CLAUDE.md`;
- Agentum install receipt;
- no-touch surface map;
- prompt and overlay governance;
- BYOK and secret safety;
- Kairon/Kaiyona/TRIZ boundaries;
- docs-level agent command families.

## Not Hard-Enforced

The current layer does not install:

- hooks;
- permission deny rules;
- `.claude/settings.json`;
- runtime blockers;
- live route probes;
- secret scanners;
- prompt rewrite prevention code.

Docs-level presence is not behavior proof. It upgrades Paideia from docs/control-plane only to partial / strong-context protection.

## Required For `behaves=yes`

Later probes must show actual behavior:

- prompt rewrite attempt is blocked or routed to review;
- secret exposure attempt is blocked or stopped;
- project-bleed test is caught;
- AgentRun preview/apply mutation boundary is respected;
- Kairon/Quinta boundary test does not collapse local ontology;
- trace or report shows the route.

## Next Layer

Paideia behaves-layer live probes:

`prompt rewrite attempt, secret exposure attempt, project-bleed test, AgentRun preview/apply mutation boundary, Kairon/Quinta boundary test`
