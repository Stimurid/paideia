# pinned to versions that actually work in production (verified on dev box)
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.17
jinja2==3.1.4

# Database
sqlite-vec==0.1.9

# LLM provider (302.ai is OpenAI-compatible)
openai==1.54.0
httpx==0.27.2  # MUST be <0.28 — newer breaks openai 1.54 (proxies kwarg)

# Frontmatter / markdown
python-frontmatter==1.1.0
PyYAML==6.0.2
markdown-it-py==3.0.0

# CLI
typer==0.13.0

# Bulk-import: file format extractors
pdfplumber==0.11.9
python-docx==1.2.0
python-pptx==1.0.2
EbookLib==0.18
beautifulsoup4==4.12.3

# Migrations
alembic==1.13.3

# Tests
pytest==8.3.3
