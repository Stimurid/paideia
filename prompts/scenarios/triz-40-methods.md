# 40 приёмов ТРИЗ под педдизайн

Перевод классических ТРИЗ-приёмов на язык образовательных систем:
разъединение → split-курс, динамизация → адаптивная траектория, посредник →
AI-роль, локальное качество → персонализация рубрик, копирование → симулятор,
матрёшка → вложенные модули, и т.д.

## Строгий JSON

```
{
  "top_methods": [
    {
      "method_name": "<имя приёма>",
      "edu_interpretation": "<как читается в педдизайне>",
      "how_applies_to_project": "<конкретная подсказка для этого проекта>",
      "corpus_examples": ["<case_id, где этот приём применён>"]
    }
  ],
  "summary": "<какой приём проекту нужен прежде всего и почему>"
}
```

Возврат top-5 приёмов.
